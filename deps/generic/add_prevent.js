addElement(
    "prevent",
    "mantencion preventiva",
    `<select name="prevent" id="prevent">
        <option value="si">Si</option>
        <option value="no">No</option>
    </select>`
);

let prevent = new Value("prevent");