addElement(
    "remote",
    "remoto",
    `<select name="remote" id="remote">
        <option value="si">Si</option>
        <option value="no">No</option>
    </select>`
);

let remote = new Value("remote");